# Dependencies
node_modules/
pnpm-store/

# Build output — main.js is committed so the community store can serve it
*.js.map

# Env & editor/local
.env
.DS_Store
.vscode
.cursor
.cursorrules
.cursorignore
.code-review-graph/

# Claude Code working state & plans — never ship in the open-source repo
.claude/
.agents/
.codex/
.remember/
.quarantine/
docs/

# Maintainer-only release tooling — not part of the published plugin
scripts/
